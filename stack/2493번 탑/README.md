# 탑

### 요구사항
왼쪽을 바라볼때 자신보다 큰 탑 중 가장 가까운 탑의 index를 체크해야한다.

### 풀이
stack을 순회하며 현재의 탑과 값을 비교하면서 같거나 작으면 stack에서 뺀다.
현재 탑보다 같거나 작은 수는 앞으로 의미가 없기 때문이다. (현재 수가 더 까가움)
stack이 비면 (= 자신보다 큰 수가 없음) 0을 저장한다.
stack이 비지 않았다면 자신보다 큰 탑이 있기에 가장 가까운 (stack.peek())을 저장한다.
현재 탑의 index를 stack에 저장한다.

### 시간복잡도
O(n)